import { styled } from "../stitches.config"

export const FormHelperText = styled("p", {
  display: "flex",
  fontSize: "$14",
  marginTop: 6,
  paddingLeft: "$xs",
  color: "$danger500",
  marginBottom: 0
})
