import { Meta, Story } from "@storybook/addon-docs"
import { Select } from "."
import { Box } from "../Box"

<Meta title="SelectComponent" component={Select} />

<Story name="Select">
  <Box css={{ display: "flex", gap: "20px" }}>
    <Box css={{ width: "300px" }}>
      <Select
        options={[
          { value: "red", label: "Red" },
          { value: "blue", label: "Blue" },
          { value: "black", label: "Black" }
        ]}
        placeholder="Select..."
      />
    </Box>
    <Box css={{ width: "300px" }}>
      <Select
        options={[
          { value: "red", label: "Red" },
          { value: "blue", label: "Blue" },
          { value: "black", label: "Black" }
        ]}
        isMulti
        placeholder="Select..."
      />
    </Box>
  </Box>
</Story>
