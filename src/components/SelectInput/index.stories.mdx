import {Meta, Story} from '@storybook/addon-docs'
import {SelectInput} from '.'
import { Box } from '../Box'

<Meta title='Select' component={SelectInput}/>

<Story name='SelectInput'>
    <Box css={{display: 'flex', gap: '20px'}}>
        <SelectInput options={[
            {value: 'red', label: 'red'},
            {value: 'blue', label: 'blue'},
            {value: 'purlple', label: 'purlple'},
            {value: 'orange', label: 'orange'}
            ]}
            placeholder='Select...'
        />
        <SelectInput options={[
            {value: 'red', label: 'red'},
            {value: 'blue', label: 'blue'},
            {value: 'purlple', label: 'purlple'},
            {value: 'orange', label: 'orange'}
            ]}
            isMulti
            placeholder='Select...'
        />
    </Box>
</Story>