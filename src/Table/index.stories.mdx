import { Meta, Story } from "@storybook/addon-docs"
import { Table, columnsExample } from "./index.tsx"
import { Box } from "../Box"

export const columnsExample = [
  columnHelper.accessor("name", {
    header: () => "Nome",
    cell: (info) => info.getValue()
  }),
  columnHelper.accessor("age", {
    header: () => "Idade",
    cell: (info) => info.renderValue()
  })
]

<Meta title="Table" component={Table} />

# Table

<Story name="Table">
  <Box css={{ width: 300 }}>
    <Table
      columns={columnsExample}
      alignment="center"
      data={[
        { name: "Pedro", age: 26 },
        { name: "José", age: 35 }
      ]}
    />
  </Box>
</Story>
