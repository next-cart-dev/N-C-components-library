import { Meta, Story } from '@storybook/addon-docs';

import { Checkbox } from './index.tsx';

<Meta title="Checkbox" component={Checkbox} />

# Checkbox

<Story name="Checkbox">
  <h1>Checkbox</h1>
  <div style={{display:'flex'}}>
    <div style={{display: 'block', marginRight: '20px'}}>
      <Checkbox label='checkbox' checkedType='checkbox' />
    </div>
    <div style={{display: 'block', marginRight: '20px'}}>
      <Checkbox label="trace"  checkedType='trace'/>
    </div>
    <div style={{display: 'block',marginRight: '20px'}}>
      <Checkbox label="disabled"  disabled />
    </div>
  </div>
</Story>
